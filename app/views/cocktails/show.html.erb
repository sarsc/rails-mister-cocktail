<div class="main-cocktail-container">
  <div class="banner-img" style="background-image: linear-gradient(rgba(226,226,226,0.3), rgba(226,226,226,0.3)), url('<%= cl_image_path @cocktail.photo unless @cocktail.photo.blank?%>')">
    <h1 ><%= @cocktail.name %></h1>
  </div>

  <div class="main-cocktail">
    <div class="ingredient-container">
      <div class=" ingredient-description">
        <div class="ingredient">
          <h3>Ingredients</h3>
        </div>
        <span class="dot">
          <%=link_to new_cocktail_dose_path(@cocktail), class: "icon-btn-large" do %>
            <i class="fas fa-plus"></i>
          <% end %>
        </span>
      </div>
      <div class="cocktail-info">
        <% @cocktail.doses.each do |dose| %>
          <div class="cocktail d-flex">
            <p><%= dose.description %> - </p>
            <p><%= dose.ingredient.name.capitalize %></p>
            <a href="">
              <%= link_to dose_path(dose), method: :delete, data: { confirm: "Are you sure?" }, class: "icon-btn" do %>
                <i class="fas fa-trash-alt"></i>
              <% end %>
            </a>
          </div>
        <% end %>
      </div>
    </div>
    <div class="cocktail-description">
      <div class="about">
        <h3>Method</h3>
      </div>
      <p> <%= @cocktail.description %> </p>
    </div>
  </div>

  <div class="btn-container d-flex justify-content-end">
   <span> <%= link_to "delete cocktail", cocktail_path(@cocktail), method: :delete, data: { confirm: "Are you sure?" }, class: "btn-w" %> </span>
  </div>

</div>

